apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: recipe-app-dev
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app.kubernetes.io/name: recipe-app
    app.kubernetes.io/instance: recipe-app-dev
    environment: dev
spec:
  project: default
  source:
    repoURL: https://github.com/kirtquist/recipe-app.git
    targetRevision: main
    path: k8s/overlays/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  revisionHistoryLimit: 10
