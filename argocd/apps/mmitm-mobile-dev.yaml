apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mmitm-mobile-dev
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app.kubernetes.io/name: mmitm-mobile
    app.kubernetes.io/instance: mmitm-mobile-dev
    environment: dev
spec:
  project: default
  source:
    repoURL: https://github.com/kirtquist/mmitm-mobile.git
    targetRevision: main
    path: k8s/overlays/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  revisionHistoryLimit: 10
